node_modules
.next
out
.git
.gitignore
README.md
Dockerfile
.dockerignore
npm-debug.log*

# never bake local env / secrets into an image — exclude ALL env files
# (incl. .env.dev / .env.live, which are supplied at runtime via --env-file),
# then re-include the committed, secret-free example.
.env*
!.env.example
