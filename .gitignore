# Claude Code — local/user-specific settings (permission overrides, etc.)
.claude/settings.local.json

# Windows / macOS metadata
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Editor state
.vs/
*.suo
*.user

# PowerSchool CSV drop zone — student/staff PII, must not be committed
data/imports/*
!data/imports/README.md
!data/imports/_generate_test_dummies.ps1
